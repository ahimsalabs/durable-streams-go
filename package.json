{
  "name": "durable-streams-go",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test:conformance": "vitest run --no-coverage"
  },
  "devDependencies": {
    "@durable-streams/client-conformance-tests": "^0.1.4",
    "@durable-streams/server-conformance-tests": "^0.1.4",
    "vitest": "^4.0.16"
  }
}
